#!/usr/bin/env lua

-- ============================================================================
-- Sketchybar Configuration Entry Point
-- ============================================================================
-- This is the main entry point for the Sketchybar configuration.
-- Sketchybar reads this file on startup and reload.
--
-- Flow:
--   1. Load helper functions and utilities (helpers/)
--   2. Initialize the main configuration (init.lua)
--
-- The configuration is written in Lua for better flexibility and
-- maintainability compared to shell scripts.
-- ============================================================================

-- Load the sketchybar-package and prepare the helper binaries
require("helpers")
require("init")
